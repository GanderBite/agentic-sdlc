{
  "wave_id": "wave-smoke",
  "verdict": "fail",
  "gates": {
    "install": {
      "passed": true,
      "command": "pnpm install --frozen-lockfile",
      "evidence": "Builder report: install PASS in 327ms; lockfile honored. Re-run skipped per reviewer scope (no install drift detected; subsequent gates ran successfully against the existing node_modules)."
    },
    "typecheck": {
      "passed": false,
      "command": "pnpm -r typecheck",
      "evidence": "apps/api tsc -b failed with 18 TS errors: 14x TS5097 (.ts import extensions in src/db/client.ts, src/db/schema.ts, src/modules/auth/repo.ts, service.ts, service.test.ts, src/seed/main.ts); TS7053 at src/modules/auth/repo.ts:84 (result[0] indexed on any[] | QueryResult<never>); TS7022/TS7024 at src/modules/auth/schema.ts:25,34 (refreshToken circular any + missing return type on relations callback). packages/contracts typecheck Done. Exit status 1."
    },
    "lint_root": {
      "passed": false,
      "command": "pnpm biome check .",
      "evidence": "Found 55 errors. Diagnostic paths confined to .relay/, .planning/, .claude/ (relay infra tooling). 128 files checked. Same outcome as builder report. Exit non-zero."
    },
    "lint_app": {
      "passed": true,
      "command": "pnpm biome check apps/ packages/",
      "evidence": "Checked 49 files in 24ms. No fixes applied. Zero diagnostics. Confirms lint failure is scope-only — apps/api and packages/contracts are clean."
    },
    "build": {
      "passed": false,
      "command": "pnpm -r build",
      "evidence": "Not re-run; build delegates to tsc -b which is the same compilation graph as the failing typecheck. All 18 TS errors block emit. Builder report confirmed FAIL with identical error list."
    },
    "test": {
      "passed": true,
      "command": "pnpm -r test",
      "evidence": "12 test files, 59/59 tests passed in 10.13s (vitest, unit + integration via testcontainers). Includes auth.login, auth.logout, auth.refresh, auth.csrf, auth.constant-time, seed.idempotent. TEST_EXIT=0."
    }
  }
}
