{
  "$schema": "https://raw.githubusercontent.com/ganderbite/relay-core/main/schemas/intel/modules.schema.json",
  "snapshot_sha": "1c1ea6393c49b62e98fdc61a77c743b222a459bc",
  "fresh_repo": true,
  "modules": [],
  "note": "Sprint-001 was reset to a clean slate (commit 1c1ea63) so the hardened workflow can re-run. `pnpm-workspace.yaml` is preserved on disk and lists `apps/*` and `packages/*` as workspace globs, but no module directories exist yet — no `package.json`, no `apps/`, no `packages/`. Modules will reappear here once the next sprint scaffolds them and `intel-refresh` is re-run."
}
