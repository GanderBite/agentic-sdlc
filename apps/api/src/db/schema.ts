// Schema barrel — Drizzle Kit reads this file as its single entry point.
// Each module schema is re-exported here so Drizzle Kit can introspect
// all tables from one import.
export * from '../modules/auth/schema.js';
